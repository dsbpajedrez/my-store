import { FormatDistanceTimePipe } from './format-distance-time.pipe';

describe('FormatDistanceTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDistanceTimePipe();
    expect(pipe).toBeTruthy();
  });
});
